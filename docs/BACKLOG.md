# Backlog

Tracked follow-up items discovered during playtesting/development that aren't
blocking the current phase's exit criteria but should be picked up soon.

## Phase 1 polish

- [x] Altitude control for the player drone (Space = up, Shift = down). Fire moved
  to left mouse click to free up Space.
- [x] Crash/collision damage for direct hull-to-hull or hull-to-terrain impacts,
  scaled by impact speed (`CrashDamage`).

## Notes

Add new items here as they come up during playtesting rather than letting them get
lost in chat history. Move resolved items to a "Done" section or just delete them
once shipped — this file is a working list, not a changelog.
